import { defineConfig } from "astro/config";

export default defineConfig({
  // サイトのベースURLを設定
  base: "/11_shimada",

  // ビルド出力ディレクトリを設定（デフォルトは 'dist'）
  outDir: "./dist/11_shimada",

  // 必要に応じて他の設定を追加
});